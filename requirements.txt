# FastAPI and ASGI server
fastapi
uvicorn

# Database
motor
python-dotenv
pydantic

# Face recognition and image processing
insightface
onnxruntime-gpu; platform_system != "Darwin"  # Use GPU if available (for Linux/Windows)
onnxruntime; platform_system == "Darwin"       # Fallback for macOS

# Image and math libraries
opencv-python-headless
numpy
scipy

# Optional: For torch-based experiments (not required by InsightFace)
torch
torchvision
